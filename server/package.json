{
  "name": "admin-base",
  "version": "1.0.0",
  "description": "Base Package for Admin Sites",
  "main": "server.ts",
  "scripts": {
    "build": "tsc",
    "build-watch": "tsc --watch",
    "start": "node --env-file .env dist/server.js",
    "dev": "nodemon --env-file .env dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "prettier": "3.2.2",
    "typescript": "^5.3.3"
  }
}
